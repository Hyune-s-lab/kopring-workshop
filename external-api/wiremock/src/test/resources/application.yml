logging:
  level:
    root: info
    com.example: debug

spring:
  cloud:
    openfeign:
      client:
        config:
          dummy-api:
            url: https://dummyapi.online/api
            loggerLevel: full
          default:
            loggerLevel: basic
